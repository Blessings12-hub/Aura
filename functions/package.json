{
  "name": "aura-functions",
  "description": "Cloud Functions for Aura — sends push notifications for match/swap/event requests via FCM, since a client can never push directly to another device.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^17.0.0"
  },
  "private": true
}
