# Appwrite Cloud Free
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=
VITE_APPWRITE_DATABASE_ID=
VITE_APPWRITE_USERS_COLLECTION_ID=users
VITE_APPWRITE_VERIFICATION_COLLECTION_ID=verificationRequests
VITE_APPWRITE_REPORTS_COLLECTION_ID=reports
VITE_APPWRITE_PRESENCE_COLLECTION_ID=presence
VITE_APPWRITE_MATCH_PAIRS_COLLECTION_ID=matchPairs
VITE_APPWRITE_MATCH_PROFILES_COLLECTION_ID=matchProfiles
VITE_APPWRITE_USER_IDENTITIES_COLLECTION_ID=userIdentities
VITE_APPWRITE_SWAP_PAIRS_COLLECTION_ID=swapPairs
VITE_APPWRITE_SKILL_SWAPS_COLLECTION_ID=skillSwaps
VITE_APPWRITE_EVENT_JOINS_COLLECTION_ID=eventJoins
VITE_APPWRITE_MEDIA_BUCKET_ID=media

# Optional: Appwrite custom domain endpoint. The cloud endpoint works, but a
# custom domain keeps session traffic on your branded origin.
# VITE_APPWRITE_ENDPOINT=https://api.example.com/v1

# Vercel hosts the Vite frontend. Appwrite owns authentication, database,
# realtime data, and private uploaded media. No Appwrite API key belongs here.

# TURN server for Skill Swap / Match video calls (see src/hooks/useVideoCall.js)
# Without these, calls fall back to a free public TURN service that isn't
# reliable at real scale. VITE_TURN_URLS can be a single URL or a
# comma-separated list (e.g. from a provider that gives you several).
# Get these from a TURN provider's dashboard (Twilio, Cloudflare Calls,
# Metered.ca, Xirsys, etc.) — no CLI needed, just their web signup.
VITE_TURN_URLS=https://global.xirsys.net
VITE_TURN_USERNAME=Blessings23
VITE_TURN_CREDENTIAL=06b72316-82ca-11f1-a968-0242ac150002
